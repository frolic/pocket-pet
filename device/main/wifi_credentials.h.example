/*
 * Copy to wifi_credentials.h (gitignored) and fill in.
 * Interim measure: the future factory app replaces this with
 * captive-portal provisioning.
 */
#pragma once

#define WIFI_SSID "your-network"
#define WIFI_PASSWORD "your-password"
